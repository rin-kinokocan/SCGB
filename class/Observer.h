/* WeakDrawable<WindowContainer> observer= */
/*   WeakDrawable<WindowContainer>(nullptr);//may not be used. */
/* virtual void UpdateObserver(int i){observer->EventObserver(i);};//may not be used. */
/* void SetObserver(WeakDrawable<WindowContainer> o){observer=o;}; */

/* pd->SetObserver(WeakDrawable<WindowContainer>(std::shared_ptr<WindowContainer>(this))); */
